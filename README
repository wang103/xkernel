      xkernel 1.0

Author: Tianyi Wang
Email:  wang103@illinois.edu


Description:
This kernel is in ELF format.


Source Tree:
arch/:      Architecture-specific source
boot/:      Bootloader related
kernel/:    Core subsystems
scripts/:   Various scripts used for building stuffs

To Compile:
Simply type 'make' to compile the kernel sources. This
will output the binary kernel 'xkernel' in ELF format.


To Run:
The kernel can be loaded using GRUB, and run using Bochs
emulator.
